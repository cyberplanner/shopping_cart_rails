<% if @products.all.any? %>
  <% @products.each do |p| %>
    <%= link_to p.name, product_path(p) %>
    £<%= p.price %>
  <% end %>
<% else %>
  <h1>no products yet</h1>
<% end %>

<%= link_to 'add product',  new_product_path %>
